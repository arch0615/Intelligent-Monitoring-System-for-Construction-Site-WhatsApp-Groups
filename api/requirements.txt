# API / Painel / Relatórios (Etapa 3)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12   # formulários do painel (POST /grupos/{id}/ativar)
jinja2>=3.1.4
apscheduler>=3.10.4        # agendamento do relatório diário
psycopg[binary]>=3.2.0     # PostgreSQL
redis>=5.2.0               # health check (heartbeats de captura/pipeline)
httpx>=0.27.2              # entrega de relatório/alerta via Telegram
python-dotenv>=1.0.1
itsdangerous>=2.2.0        # cookies de sessão (login) via SessionMiddleware
anthropic>=0.69.0          # resumo executivo do histórico via Claude (spec #3)
